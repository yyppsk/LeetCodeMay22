#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
void generate(vector<int> sets, )
int countVowelStrings(int n)
{   vector<char> vowels = {'a','e','i','o','u'};
    vector<int> sets;
    generate(sets,n,vowels);
}
int main()
{   
    countVowelStrings(2);
    return 0;
}